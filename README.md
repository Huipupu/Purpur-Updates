# Purpur Updates

Public update feed and installer releases for Purpur.

The main Purpur source repository can stay private. The plugin only reads
`update.json` from this repository to determine whether a newer installer is
available.

